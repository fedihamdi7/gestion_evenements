server:
  port: 8761                      # Standard Eureka port. Dashboard: http://localhost:8761

spring:
  application:
    name: eureka-server

eureka:
  client:
    # This IS the registry, so it does not register with itself nor fetch a registry from anywhere.
    register-with-eureka: false
    fetch-registry: false
  instance:
    hostname: localhost
